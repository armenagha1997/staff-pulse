CLIENT_PORT=8080

# Optional: if set, AI search delegates natural-language parsing to Claude
# instead of the built-in offline heuristic parser. Leave empty to run
# entirely offline (docker-compose up works with no keys at all).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
